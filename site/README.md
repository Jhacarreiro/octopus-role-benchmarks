# Site

Static public UI will live here. The homepage will list Octopus roles and benchmark-per-dollar comparisons; methodology pages will expose the exact active benchmark components, weights, coverage and source timestamps.
